

# --- Core web ---------------------------------------------------------------
flask>=3.0.0
flask-cors>=4.0.0                # simple CORS, lighter than Smorest for now
flask-smorest>=0.42.0            # API documentation and validation

# --- Data wrangling ---------------------------------------------------------
numpy>=1.24.0
pandas>=2.0.0                    # Updated for Python 3.13 compatibility

# --- Forecasting ------------------------------------------------------------
prophet>=1.1.0                   # pre-built wheels for Py<=3.11
scikit-learn>=1.3.0              # Updated for Python 3.13 compatibility
scipy>=1.10.0                    # Updated for Python 3.13 compatibility

# --- Optional visual / notebook usage --------------------------------------
plotly>=5.0.0
streamlit>=1.25.0
matplotlib>=3.7.0                # last version with wheels on macOS arm64

# --- Dev / test -------------------------------------------------------------
pytest>=7.0.0
pytest-cov>=4.0.0                # coverage reporting
black>=23.0.0
ruff>=0.1.0

# --- Production deployment --------------------------------------------------
gunicorn>=21.0.0                 # WSGI server for production
python-dotenv>=1.0.0             # environment variable management

# --- Additional utilities ---------------------------------------------------
werkzeug>=3.0.0                  # WSGI utilities
marshmallow>=3.20.0              # data validation and serialization